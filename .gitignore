# Python virtual environments
venv/
.venv/
__pycache__/
*.pyc
.env

# Playwright browser profile (persistent login state)
.playwright-profile/
.playwright-profile-batch-*/

# Batch run outputs
batch_results/

# Test outputs (not versioned - prompts are versioned, not results)
test_results/

# IDE
.vscode/
.idea/

# Secrets (NEVER commit - contains app_id and secret_key)
config.json

# OS
.DS_Store

# Note: prompts_config.json IS versioned (contains prompt IDs, no secrets)
